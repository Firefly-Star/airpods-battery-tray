<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 自己管主题，这里只需要一个不带 ActionBar 的窗口样式。 -->
    <style name="Theme.AirPodsBattery" parent="@android:style/Theme.DeviceDefault.NoActionBar" />
</resources>
